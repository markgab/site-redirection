import { LitElement, html, css } from 'lit';
import { customElement } from 'lit/decorators';

@customElement('site-redirection')
export class SiteRedirection extends LitElement {
    render() {
        return html`
            <fluent-button>ICON</fluent-button>
        `;
    }

    static styles = css`
    
    `;
}